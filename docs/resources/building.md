---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "satisfactory_building Resource - satisfactory"
subcategory: ""
description: |-
  A production machine (constructor, smelter, assembler, ...) placed in the world.
---

# satisfactory_building (Resource)

A production machine (constructor, smelter, assembler, ...) placed in the world.

## Example Usage

```terraform
resource "satisfactory_building" "smelter" {
  class  = "Build_SmelterMk1_C"
  x      = 200
  y      = 0
  z      = 20200
  recipe = "Recipe_IngotIron_C"
  # clock_speed defaults to 1.0 on create for manufacturer classes.
  clock_speed = 1.0
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `class` (String) Buildable class name, e.g. Build_ConstructorMk1_C or Build_SmelterMk1_C.
- `x` (Number) World X in centimetres.
- `y` (Number) World Y in centimetres.
- `z` (Number) World Z in centimetres.

### Optional

- `clock_speed` (Number) Clock speed as a fraction (1.0 = 100%). Changeable in place. Only meaningful for manufacturer buildings (constructors, smelters, ...); the mod omits this field entirely for other classes (splitters, power poles, ...), so it can't have a static default without breaking those - defaults to 1.0 on create for classes that do support it (see Create).
- `recipe` (String) Recipe class name, e.g. Recipe_IronPlate_C. Changeable in place.
- `yaw` (Number) Rotation around the vertical axis in degrees.

### Read-Only

- `id` (String) Terraform-assigned stable ID, persisted in the save game by the mod.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# The import ID is the resource's tf_id (the UUID the mod persists in the save).
terraform import satisfactory_building.smelter "0a1b2c3d-4e5f-6789-abcd-ef0123456789"
```
